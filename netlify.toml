[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  
# Ensure middleware runs on Netlify
[functions]
  node_bundler = "esbuild"

